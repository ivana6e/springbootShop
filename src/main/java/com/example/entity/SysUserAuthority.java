package com.example.entity;

public enum SysUserAuthority {
	
	ADMIN, USER;

	/* public SysUserAuthority fromString(String key) {
		for(SysUserAuthority auth: SysUserAuthority.values()) {
			if(auth.name().equalsIgnoreCase(key)) {
				return auth;
			}
		}
		return null;
	}
	*/

}
